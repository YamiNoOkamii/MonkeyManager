head
style
  include css/style.css
  include css/button.css
  include css/home.css
  title= title


body
  h1= message
  a(href = "/") Retour
  h4 Pour modifier un singe cliquez dessus, pour le Supprimer appuyez sur le bouton
  - var name = ""
  ul
    for Monkey in Monkeys
      - name = Monkey.Monkeyname
      form(method="delete" action="/Monkey/"+Monkey.id)
        li
          a(href="/modifmonkey/"+Monkey.id) #{name}
            button#id.form-control(name='#{Monkey.id}') Abattre